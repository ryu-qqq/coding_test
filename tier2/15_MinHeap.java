/**
 * Min Heap (Priority Queue) 직접 구현
 *
 * 문제: 배열 기반 이진 힙 구현 (Java PriorityQueue 사용 금지)
 *
 * --- 인터페이스 ---
 *   insert(val)
 *   extractMin(): int
 *   peek(): int
 *   size(): int
 *
 * --- 시간복잡도 목표 ---
 *   insert: O(log n)
 *   extractMin: O(log n)
 *   peek: O(1)
 *   buildHeap (배치): O(n)  (도전 과제)
 *
 * --- 핵심 아이디어 ---
 *   (스스로 떠올려볼 것)
